[package]
name = "buildpacks-apt"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
libcnb = { version = "=0.17.0", features = ["trace"] }

[dev-dependencies]
libcnb-test = "=0.17.0"

[profile.release]
strip = true